ecommerce_dbt:
  outputs:
    dev:
      type: postgres
      host: postgres
      port: 5432
      dbname: "{{ env_var('POSTGRES_DB') }}"
      schema: public
      user: "{{ env_var('POSTGRES_USER') }}"
      password: "{{ env_var('POSTGRES_PASSWORD') }}"
      threads: 4
  target: dev
